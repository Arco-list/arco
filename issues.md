✅ All identified issues have been addressed.

- Corrected the "Wellness" service spelling and slug across the UI, seeds, icons, and database records.
- Locked down `mv_professional_summary` access so Plus-only enforcement cannot be bypassed and updated `search_professionals` to use full-text search.
- Updated `useProfessionalsQuery` to eliminate redundant dependencies, support paginated loading, and cancel in-flight requests.
